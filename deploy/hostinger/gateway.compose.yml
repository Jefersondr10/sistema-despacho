name: infra-gateway

services:
  caddy:
    image: caddy:2.11.4-alpine
    restart: unless-stopped
    environment:
      BASE_DOMAIN: "${BASE_DOMAIN:?Configure BASE_DOMAIN}"
      BIPAGEM_SUBDOMAIN: "${BIPAGEM_SUBDOMAIN:-bipagem}"
      FISCAL_SUBDOMAIN: "${FISCAL_SUBDOMAIN:-nfe}"
      LEGACY_BIPAGEM_DOMAIN: "${LEGACY_BIPAGEM_DOMAIN:-bipagem.85-31-63-215.sslip.io}"
      LEGACY_FISCAL_DOMAIN: "${LEGACY_FISCAL_DOMAIN:-notas.85-31-63-215.sslip.io}"
    ports:
      - "80:80"
      - "443:443"
    configs:
      - source: caddyfile
        target: /etc/caddy/Caddyfile
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - edge
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s

configs:
  caddyfile:
    content: |
      ${LEGACY_BIPAGEM_DOMAIN:-bipagem.85-31-63-215.sslip.io}, ${BIPAGEM_SUBDOMAIN:-bipagem}.${BASE_DOMAIN:?Configure BASE_DOMAIN} {
        encode zstd gzip

        header {
          -Server
          X-Content-Type-Options "nosniff"
          Referrer-Policy "strict-origin-when-cross-origin"
          Strict-Transport-Security "max-age=31536000; includeSubDomains"
        }

        reverse_proxy bipagem-web:3000
      }

      ${LEGACY_FISCAL_DOMAIN:-notas.85-31-63-215.sslip.io}, ${FISCAL_SUBDOMAIN:-nfe}.${BASE_DOMAIN:?Configure BASE_DOMAIN} {
        encode zstd gzip

        header {
          -Server
          X-Content-Type-Options "nosniff"
          Referrer-Policy "strict-origin-when-cross-origin"
          Strict-Transport-Security "max-age=31536000; includeSubDomains"
        }

        reverse_proxy fiscal-web:8080
      }

networks:
  edge:
    external: true
    name: edge

volumes:
  caddy_data:
  caddy_config:
