{$BIPAGEM_DOMAIN} {
  encode zstd gzip

  header {
    -Server
    X-Content-Type-Options "nosniff"
    Referrer-Policy "strict-origin-when-cross-origin"
  }

  reverse_proxy bipagem-web:3000
}

# Para publicar o sistema fiscal no futuro, conecte o serviço fiscal-web à
# rede edge e adicione uma segunda rota para fiscal-web:8080 neste arquivo.
